import { brandPalette, productBrand } from "@nexus/branding";

export { productBrand };

export const webTokens = {
  phoneNavigationHeight: 64,
  minimumTouchTarget: 44,
  readingWidth: 760,
  accent: brandPalette.violet,
  live: brandPalette.cyan,
} as const;
